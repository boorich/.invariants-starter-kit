<!-- ·NAV:M -->
# Implementation map

> **Navigational markdown** (`·NAV:M`) — not in `vecs`. Not governance (that is `/.invariants`).
>
> This file **points at code** that materializes the interface. The agent **may edit** this file and commit; that is expected.
>
> **Find code:** `vecs query <collection> "…"` — code-only corpus.  
> **Record where it lives:** update this map on disk after you have read the actual source files.
>
> Provenance: `git log IMPLEMENTATION_MAP.md` — each commit should reference the issue that triggered the change.

Only in-scope interface concerns belong here.

---

## Example concern (delete when you add real entries)

**What this is about:** _(the seam — e.g. how identity is derived across SDKs)_

**Governed by:** `example_frozen_cross_repo_rule` in `/.invariants`

**Where the code lives:**

- _(paths added by agent or humans after first triage)_

**Contract reference:** `CONTRACT.md`
